// M0
// Placeholder to give the LingoPodKit target a compiled source file
// independent of Models/. Safe to delete the enum below once real logic
// (Feeds/, Transcripts/) exists, or leave it — it is harmless.

public enum LingoPodKit {
    /// Package version marker; bump has no build effect, useful for sanity
    /// checks in tests.
    public static let scaffoldVersion = "0.1.0"
}
